`struct_entrada_directorio` deberia tener un nombre, de tamaño fijo, la direccion del bloque inicial y el tamaño del archivo. 

```c
struct entrada_directorio {
        char name[256];
        uint initial_block_adress;
        uint file_size;
}
```

```c
cargar_archivo(char *directorios[]){
    // Primero parseo root para conseguir el block_adress del primer elemento
    int* raw_data =  root_table();
    for(int i = 0; i < sizeOf(directorios); i++){
        // Obs que nunca voy a entrar con el ultimo archivo como raw_data. Porque en  i = sizeOf(directorios) - 1 tengo en raw_data el directorio que contiene el archivo, y en el ciclo lo que voy a hacer va a ser encontrar el archivo dentro del directorio. 
        struct entrada_directorio entradas_dir_actual[] = parse_directory_entries(raw_data);
        for(int j = 0; j < sizeOf(entradas_dir_actual); j++){
            if(entradas_dir_actual[j].name == directorios[i]){
                // Una vez que encontramos el directorio hay que armar una lista con los bloques que componen al directorio/ archivo. Agrego los indices de la fat de ese bloque en particular.
                uint* puntero = initial_block_adress;
                // En la realidad habria que usar malloc, realloc y free, pero me da fiaca. Tambien el manejo de punteros es dudoso at best.
                block_adresses = [];
                for(int x = 0; x < ceil(entradas_dir_actual[j].file_size/BLOCK_SIZE); x+= BLOCK_SIZE){
                    raw_data.append(FAT_ENTRY(puntero)); 
                    puntero += 1; 
                }
                raw_data = read_blocks(block_adresses);
                break;
            }
        }
    }
    return raw_data; 
}
```